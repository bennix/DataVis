# DataVis

这个项目提供一个基于 Streamlit 的数据可视化应用，可以上传 CSV 或 JSON 文件，选择字段生成常见图表，并查看数据的简单统计结果。应用针对中文显示进行了处理。

## 使用方式

1. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```
2. 运行应用：
   ```bash
   streamlit run app.py
   ```
3. 在浏览器中按照页面提示上传数据文件并选择需要可视化的字段和图表类型。
